<div class="thumbnail">
  <%= image_tag(business.image.url(:medium), heigth: 200, width: 200) %>

  <div class="caption">
    <h3>
      <%= link_to business.name, business_path(business) %>
    </h3>
  </div>
</div>
