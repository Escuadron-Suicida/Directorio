%table.table
  %thead
    -# %th
    -#   Leido
    %th
      De
    %th
      Fecha

  %tbody
    - @messages.each do |message|
      %tr
        -# %td
          -# = message.seen? ? "Si" : "No"
        %td
          = link_to message.sender.name, message_path(message)
        %td
          = message.created_at
