<div class="thumbnail">
  <%= image_tag(product.avatar.url(:medium), heigth: 200, width: 200) %>

  <div class="caption">
    <h3>
      <%= link_to product.name, product_path(product) %>
    </h3>
  </div>
</div>
